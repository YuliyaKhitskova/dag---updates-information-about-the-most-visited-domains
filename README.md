# dag-updates-information-about-the-most-visited-domains
Shows the update schedule for the domains that make the most purchases
The DAG contains tasks that automatically update calculations at a specified time.
